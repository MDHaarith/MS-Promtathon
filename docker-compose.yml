version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PING_MESSAGE: pong
    # Add any other environment variables your app needs
    # volumes:
    #   - ./server:/app/server  # For development with hot reload, use the dev docker setup instead
